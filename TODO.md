# TODO

## Features

- [ ] **Permission Model** - Start in read-only mode by default. When a mutating tool is invoked, prompt the user for confirmation before executing. Similar to Claude Code's permission flow. Consider annotating tools to mark which ones require user approval.
